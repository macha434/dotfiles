# CLAUDE.md

このリポジトリで作業する際の指示。

## vscode/

`vscode/keybindings.json` のキーバインド、または `vscode/settings.json` の
Vim キーマップ (`vim.*KeyBindings*`) を追加・変更・削除したときは、
必ず `vscode/CHEATSHEET.md` も同じ内容に合わせて更新すること。

`CHEATSHEET.md` は `ctrl+a shift+/` で表示される自作キーバインドの一覧で、
自動生成ではなく手動でメンテしているため、更新を忘れるとバインドの実体と
チートシートの内容がズレる。
