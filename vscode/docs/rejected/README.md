# 採用しなかったキーバインド・設定

検討はしたものの `vscode/keybindings.json` / `vscode/settings.json` に入れなかった
キーバインド・設定の記録。目的はコードの正ではなく、**なぜそうしなかったか**を残すこと
(採用理由ではなく不採用理由のログ)。

- ファイル名: コマンド ID または設定キー (例: `workbench.action.gotoLine.md`)
- 内容: 何を検討したか / なぜ不採用にしたか / その根拠
