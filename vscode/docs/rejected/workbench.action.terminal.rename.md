# workbench.action.terminal.rename

## 検討していた内容

tmux の `rename-window` (`ctrl+a ,`) に寄せて、ターミナルに名前を付けられる
キーバインドを検討した。

## 不採用の理由

実際の作業でターミナルに名前を付けたいと思ったことがない。使わない機能に
キーを割り当てる必要はない。

## 根拠

本人の作業実態による判断。
